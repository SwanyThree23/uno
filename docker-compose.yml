version: '3.8'

services:
  antigravity:
    build: .
    ports:
      - "4000:4000"
    env_file: .env
    volumes:
      - .:/app
      - /app/node_modules
      - antigravity_data:/app/data
    restart: unless-stopped

volumes:
  antigravity_data:
